class Vertex:

    def __init__(self, vertex_id, name, address, city, state, zipcode):
        self.vertex_id = int(vertex_id)
        self.name = name
        self.address = address
        self.city = city
        self.state = state
        self.zipcode = zipcode
